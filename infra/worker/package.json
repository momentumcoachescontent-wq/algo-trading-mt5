{
  "name": "trading-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "rm -rf .tmp-test && tsc src/index.ts src/observability.ts src/tradeCandidate.ts src/lifecycle.ts --target ES2022 --module commonjs --moduleResolution node --outDir .tmp-test --strict --skipLibCheck && node --test test/observability.test.cjs test/lifecycle-handler.test.cjs",
    "typecheck": "tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler --lib ES2022,WebWorker --strict --skipLibCheck src/index.ts src/observability.ts src/tradeCandidate.ts src/lifecycle.ts"
  },
  "dependencies": {
    "hono": "4.12.30"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "wrangler": "4.118.0"
  }
}
